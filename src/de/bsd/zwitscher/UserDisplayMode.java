package de.bsd.zwitscher;

/**
 * Enum that holds the possible ways to display user info in timelines
 *
 * @author Heiko W. Rupp
 */
public enum UserDisplayMode {
    USER,
    SCREENNAME,
    BOTH
}
